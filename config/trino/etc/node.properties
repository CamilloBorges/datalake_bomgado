# =============================================================================
# Trino — Configuração do nó (node.properties)
# =============================================================================

node.environment=development
node.id=trino-coordinator-1
node.data-dir=/data/trino
